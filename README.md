# Dungeon Cards

Standalone card-based tabletop adventure platform.

## Product boundary

This repository contains only Dungeon Cards. It does not depend on or embed the
DungeonCards rules-reference website.

## Run locally

Serve the repository root with any static server:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

